<view>
  <view class="huo_index" wx:for="{{act}}" wx:key="index">
    <image src="{{item.list_img}}" class="huodong" bindtap="xiangq" data-activity_id="{{item.activity_id}}"></image>
    <view class="type" wx:if="{{item.is_bm==1}}">已报名</view>
  </view>
</view>