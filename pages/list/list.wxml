<!--pages/list/list.wxml-->
<!-- <text>pages/list/list.wxml</text> -->
<view class="list_box">
  <view wx:for="{{question_lst}}" wx:key="index" class="list_li {{item.is_tj==0?'list_hei':'list_hui'}}" bindtap="zuoti" data-id="{{item.id}}" data-sorts="{{item.sorts}}">
    {{item.ti_tree}}
  </view>
</view>